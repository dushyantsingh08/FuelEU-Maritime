# Frontend Placeholder

This directory is reserved for the future frontend application.

Suggested next steps:
- Define the chosen framework (e.g. React, Vue, Svelte).
- Initialize a build tool (e.g. Vite, Next.js).
- Configure communication with the backend API in `../backend`.


